CC=g++
EXE=main.out
HDR=../include
CPPFLAGS=-g -I $(HDR) -std=c++11 -D_USE_POOL_ALLOC


SRC=$(wildcard ../source/*.cpp) $(wildcard *.cpp)
OBJ=$(patsubst %.cpp,%.o,$(SRC))
DEP=$(patsubst %.o,%.d,$(OBJ))


$(EXE): $(OBJ)
	$(CC) $^ $(CPPFLAGS) -o $@


%.o: %.cpp
	$(CC) $< -c $(CPPFLAGS) -o $@


include $(DEP)


%.d: %.cpp
	set -e; \
	rm -f $@; \
	$(CC) $< -MM $(CPPFLAGS) > $@.tmp; \
	sed 's,.*:,$*.o $@:,g' < $@.tmp > $@; \
	rm -f $@.tmp


.PHONY: clean
clean:
	rm -f $(EXE) $(OBJ) $(DEP)


echo:
	@echo SRC=$(SRC)  
	@echo OBJ=$(OBJ)  
	@echo dep=$(DEP)  
	@echo CPPFLAGS=$(CPPFLAGS) 
